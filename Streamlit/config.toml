# Tema escuro fixo — sem este arquivo na raiz do repositório, o Streamlit
# Cloud usa o padrão claro e o app perde a identidade visual (foi o que
# aconteceu no deploy anterior).

[theme]
base = "dark"
primaryColor = "#3B82F6"
backgroundColor = "#0B0F19"
secondaryBackgroundColor = "#0D1220"
textColor = "#E4E9F2"
font = "sans serif"
